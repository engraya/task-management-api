services:
  postgres-test:
    image: postgres:17-alpine
    profiles: [test]
    environment:
      POSTGRES_DB: task_management_test
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "127.0.0.1:5433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d task_management_test"]
      interval: 2s
      timeout: 5s
      retries: 15

  postgres:
    image: postgres:17-alpine
    container_name: task-management-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: task_management
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d task_management"]
      interval: 5s
      timeout: 5s
      retries: 10

  api:
    build: .
    container_name: task-management-api
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      NODE_ENV: production
      PORT: 3000
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/task_management?schema=public
      JWT_SECRET: replace-this-with-a-long-random-secret
      JWT_EXPIRES_IN: 15m
      CORS_ORIGIN: http://localhost:3000
    ports:
      - "3000:3000"
    command: sh -c "npm run prisma:migrate:deploy && node dist/main.js"

volumes:
  postgres_data:
